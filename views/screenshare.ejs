<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Meet - Vikas Sharma Scrum Meeting</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    body {
      background-color: #202124;
      color: white;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      overflow: hidden;
    }

    /* Main Layout */
    .main-container {
      display: flex;
      height: 100vh;
      gap: 8px;
      padding: 8px;
    }

    /* Screen Share */
    .screen-share-container {
      flex: 70%;
      background: black;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 12px;
      position: relative;
    }

    .screen-share-container i {
      font-size: 50px;
      color: white;
    }

    /* Participants Grid */
    .participants-container {
      flex: 30%;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 10px;
      padding: 10px;
      overflow-y: auto;
      background: #3C4043;
      border-radius: 12px;
    }

    /* Video Tile */
    .video-tile {
      background: #5F6368;
      border-radius: 15px;
      padding: 10px;
      position: relative;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .video-tile img {
      width: 100%;
      height: 100px;
      object-fit: cover;
      border-radius: 10px;
    }

    .initials {
      width: 60px;
      height: 60px;
      background-color: gray;
      border-radius: 50%;
      font-size: 22px;
      display: flex;
      justify-content: center;
      align-items: center;
      font-weight: bold;
      margin-bottom: 5px;
    }

    .mute-icon {
      position: absolute;
      top: 10px;
      right: 10px;
      color: white;
      background: rgba(0, 0, 0, 0.5);
      padding: 5px;
      border-radius: 50%;
      font-size: 14px;
    }

    /* Bottom Controls */
    .bottom-controls {
      position: fixed;
      bottom: 10px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      background: rgba(0, 0, 0, 0.8);
      border-radius: 30px;
      padding: 10px;
      gap: 15px;
    }

    .bottom-controls button {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      background-color: #5F6368;
      border: none;
      font-size: 18px;
      transition: 0.3s;
    }

    .bottom-controls button:hover {
      background-color: #757575;
    }

    .bottom-controls .btn-danger {
      background-color: #EA4335;
    }

    /* Badge Count */
    .badge-count {
      background-color: #5F6368;
      color: white;
      font-size: 12px;
      padding: 5px 8px;
      border-radius: 10px;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .main-container {
        flex-direction: column;
      }

      .screen-share-container {
        flex: 1;
        height: 60vh;
      }

      .participants-container {
        flex: 1;
        height: 40vh;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
      }

      .bottom-controls {
        flex-wrap: wrap;
        justify-content: center;
      }

      .bottom-controls button {
        width: 40px;
        height: 40px;
        font-size: 16px;
      }
    }
  </style>
</head>

<body>

  <div class="main-container">
    <!-- Screen Share -->
    <div class="screen-share-container">
      <div>
        <i class="fa-solid fa-display"></i>
        <p>Screen Sharing...</p>
      </div>
    </div>

    <!-- Participants -->
    <div class="participants-container">
      <% let users=[ 
      { name: "Ahmed Hashmi" }, { name: "Mukesh Kumar" }, { name: "Rudhir Ingale" }, {
        name: "Vikas Sharma" }, { name: "Vicky Sharma" }, { name: "Angel Priya" } ,
      { name: "Ahmed Hashmi" }, { name: "Mukesh Kumar" }, { name: "Rudhir Ingale" }, {
        name: "Vikas Sharma" }, { name: "Vicky Sharma" }, { name: "Angel Priya" } 
        ]; %>

        <% users.forEach((user, index)=> { %>
          <div class="video-tile">
            <div class="initials">
              <%= user.name.charAt(0) %>
            </div>
            <p>
              <%= user.name %>
            </p>
            <span class="mute-icon"><i class="fas fa-microphone-slash"></i></span>
          </div>
          <% }); %>
    </div>
  </div>

  <!-- Bottom Controls -->
  <div class="bottom-controls">
    <button class="btn" id="global-mute">
      <i class="fas fa-microphone-slash"></i>
    </button>
    <button class="btn"><i class="fa-solid fa-video-slash"></i></button>
    <button class="btn"><i class="fa-solid fa-arrow-up-from-bracket"></i></button>
    <button class="btn"><i class="fa-solid fa-comment"></i></button>

    <!-- User Count -->
    <button class="btn position-relative">
      <i class="fa-solid fa-users"></i>
      <span class="badge badge-count position-absolute top-0 start-100 translate-middle">6</span>
    </button>
    <button class="btn btn-danger">
      <i class="fa-solid fa-phone"></i>
    </button>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>